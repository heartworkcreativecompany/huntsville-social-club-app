# Copy to `.env.local` and fill in values for local development.
# See `.env.example` for the full variable list (Supabase, Stripe Checkout, Stripe Identity).
#
# Identity verification uses Stripe Identity with:
#   STRIPE_SECRET_KEY
#   STRIPE_WEBHOOK_SECRET
# (Enable Identity webhook events listed in `.env.example`.)
